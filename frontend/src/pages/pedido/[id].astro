---
import BaseLayout from '../../layouts/BaseLayout.astro';

export const prerender = false;

const { id } = Astro.params;
---

<BaseLayout title={`Pedido #${id} - Mimoto`}>
  <section class="max-w-3xl mx-auto px-4 py-12 md:py-16 space-y-8">
    <div class="text-center mb-8">
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-2">Estado del pedido #{id}</h1>
      <div class="w-24 h-1 bg-primary mx-auto"></div>
    </div>
    
    <div class="bg-white border-2 border-primary rounded-xl p-8 shadow-lg space-y-4">
      <p class="text-gray-700 leading-relaxed">
        Aquí se mostrará el detalle y el estado actual de tu pedido. Por ahora, puedes
        consultar el estado con nuestro equipo mientras se valida el pago.
      </p>
      
      <div class="bg-gray-50 border-2 border-gray-200 rounded-lg p-6 space-y-3">
        <p class="text-gray-700">
          Estado actual:
          <span class="font-bold text-primary ml-2">waiting_confirmation</span>
        </p>
        <p class="text-gray-600 text-sm">
          Una vez confirmado el pago, actualizaremos el estado de tu pedido y te
          contactaremos para coordinar la entrega o retiro.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
