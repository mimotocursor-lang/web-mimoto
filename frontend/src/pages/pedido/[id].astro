---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { supabaseClient } from '../../lib/supabase/client';

// Esta ruta no se pre-renderiza porque los pedidos son dinámicos
export async function getStaticPaths() {
  return [];
}

const { id } = Astro.params;

// Obtener datos del pedido desde la base de datos
let order = null;
let orderItems = [];
let orderError = null;

if (id) {
  try {
    const { data, error } = await supabaseClient
      .from('orders')
      .select('id, total_amount, status, payment_reference, payment_details, created_at, updated_at, email, phone, rut, first_name, last_name, address_street, address_number, address_apartment, address_city, address_commune')
      .eq('id', id)
      .single();

    if (!error && data) {
      order = data;
      
      // Obtener items del pedido
      const { data: items, error: itemsError } = await supabaseClient
        .from('order_items')
        .select('id, product_id, quantity, unit_price, total_price, products(name)')
        .eq('order_id', id);
      
      if (!itemsError && items) {
        orderItems = items;
      }
    } else {
      orderError = error;
    }
  } catch (err) {
    console.error('Error obteniendo el pedido:', err);
    orderError = err;
  }
}
---

<BaseLayout title={`Pedido #${id} - Mimoto`}>
  <section class="max-w-3xl mx-auto px-4 sm:px-6 py-8 sm:py-12 md:py-16 space-y-6 sm:space-y-8">
    <div class="text-center mb-6 sm:mb-8">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-2">Estado del pedido #{id}</h1>
      <div class="w-16 sm:w-24 h-1 bg-primary mx-auto"></div>
    </div>
    
    {order ? (
      <div class="bg-gray-900/50 border-2 border-primary rounded-xl p-4 sm:p-6 md:p-8 shadow-lg space-y-4 sm:space-y-6 backdrop-blur-sm">
        <div class="bg-gray-800/50 border-2 border-primary/30 rounded-lg p-4 sm:p-6 space-y-3 sm:space-y-4">
          <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-2">
            <span class="text-white/90 font-semibold text-sm sm:text-base">Estado:</span>
            <span class={`font-bold text-lg sm:text-xl ${
              (order.status === 'paid' || (order.payment_details && typeof order.payment_details === 'object' && (order.payment_details.responseCode === 0 || order.payment_details.authorizationCode))) ? 'text-green-500' :
              order.status === 'pending_payment' || order.status === 'pending' ? 'text-yellow-500' :
              order.status === 'cancelled' ? 'text-red-500' :
              'text-primary'
            }`}>
              {(order.status === 'paid' || (order.payment_details && typeof order.payment_details === 'object' && (order.payment_details.responseCode === 0 || order.payment_details.authorizationCode))) ? '✅ Pagado' :
               order.status === 'pending_payment' || order.status === 'pending' ? '⏳ Pendiente de pago' :
               order.status === 'cancelled' ? '❌ Cancelado' :
               order.status === 'waiting_confirmation' ? '⏳ Esperando confirmación' :
               order.status}
            </span>
          </div>
          
          <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center border-t border-primary/30 pt-3 sm:pt-4 gap-2">
            <span class="text-white/90 font-semibold text-sm sm:text-base">Monto total:</span>
            <span class="font-bold text-primary text-lg sm:text-xl">${Number(order.total_amount).toLocaleString('es-CL')}</span>
          </div>
          
          {order.payment_reference && (
            <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start border-t border-primary/30 pt-3 sm:pt-4 gap-2">
              <span class="text-white/90 font-semibold text-sm sm:text-base flex-shrink-0">Referencia de pago:</span>
              <span class="font-mono text-xs sm:text-sm text-primary break-all text-left sm:text-right">{order.payment_reference}</span>
            </div>
          )}
          
          {order.payment_details && typeof order.payment_details === 'object' && (
            <div class="border-t border-primary/30 pt-3 sm:pt-4 space-y-2">
              <span class="text-white/90 font-semibold block mb-2 text-sm sm:text-base">Detalles del pago:</span>
              {order.payment_details.authorizationCode && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Código de autorización:</span>
                  <span class="text-primary font-mono text-xs sm:text-sm break-all text-left sm:text-right">{order.payment_details.authorizationCode}</span>
                </div>
              )}
              {order.payment_details.responseCode !== undefined && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Código de respuesta:</span>
                  <span class={`font-mono text-xs sm:text-sm ${order.payment_details.responseCode === 0 ? 'text-green-500' : 'text-red-500'} text-left sm:text-right`}>
                    {order.payment_details.responseCode}
                  </span>
                </div>
              )}
              {order.payment_details.transactionDate && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Fecha de transacción:</span>
                  <span class="text-white/70 text-xs sm:text-sm text-left sm:text-right">{new Date(order.payment_details.transactionDate).toLocaleString('es-CL')}</span>
                </div>
              )}
            </div>
          )}
          
          <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center border-t border-primary/30 pt-3 sm:pt-4 gap-2">
            <span class="text-white/90 font-semibold text-sm sm:text-base">Fecha de creación:</span>
            <span class="text-white/70 text-xs sm:text-sm">{new Date(order.created_at).toLocaleString('es-CL')}</span>
          </div>
          
          {order.updated_at && order.updated_at !== order.created_at && (
            <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center border-t border-primary/30 pt-3 sm:pt-4 gap-2">
              <span class="text-white/90 font-semibold text-sm sm:text-base">Última actualización:</span>
              <span class="text-white/70 text-xs sm:text-sm">{new Date(order.updated_at).toLocaleString('es-CL')}</span>
            </div>
          )}
        </div>
        
        {/* Información del cliente (si es invitado) */}
        {(order.email || order.phone || order.rut || order.first_name || order.last_name || order.address_street) && (
          <div class="bg-gray-800/50 border-2 border-primary/30 rounded-lg p-4 sm:p-6 space-y-3 sm:space-y-4">
            <h3 class="text-white font-bold text-base sm:text-lg mb-3 sm:mb-4">Información del Cliente</h3>
            <div class="space-y-2 sm:space-y-3">
              {(order.first_name || order.last_name) && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Nombre completo:</span>
                  <span class="text-white text-xs sm:text-sm text-left sm:text-right">{(order.first_name || '') + ' ' + (order.last_name || '')}</span>
                </div>
              )}
              {order.rut && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">RUT:</span>
                  <span class="text-white text-xs sm:text-sm text-left sm:text-right">{order.rut}</span>
                </div>
              )}
              {order.email && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Email:</span>
                  <span class="text-white text-xs sm:text-sm text-left sm:text-right">{order.email}</span>
                </div>
              )}
              {order.phone && (
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-1 sm:gap-0">
                  <span class="text-white/70 text-xs sm:text-sm flex-shrink-0">Teléfono:</span>
                  <span class="text-white text-xs sm:text-sm text-left sm:text-right">{order.phone}</span>
                </div>
              )}
              {(order.address_street || order.address_number || order.address_city || order.address_commune) && (
                <div class="border-t border-primary/30 pt-3 sm:pt-4 mt-3 sm:mt-4">
                  <span class="text-white/70 text-xs sm:text-sm font-semibold block mb-2">Dirección de entrega:</span>
                  <div class="space-y-1">
                    {order.address_street && order.address_number && (
                      <div class="text-white text-xs sm:text-sm">
                        {order.address_street} {order.address_number}{order.address_apartment ? ', ' + order.address_apartment : ''}
                      </div>
                    )}
                    {order.address_commune && (
                      <div class="text-white text-xs sm:text-sm">Comuna: {order.address_commune}</div>
                    )}
                    {order.address_city && (
                      <div class="text-white text-xs sm:text-sm">Ciudad: {order.address_city}</div>
                    )}
                  </div>
                </div>
              )}
            </div>
          </div>
        )}
        
        {orderItems.length > 0 && (
          <div class="bg-gray-800/50 border-2 border-primary/30 rounded-lg p-4 sm:p-6 space-y-2 sm:space-y-3">
            <h3 class="text-white font-bold text-base sm:text-lg mb-3 sm:mb-4">Items del pedido:</h3>
            {orderItems.map((item: any) => (
              <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center p-3 bg-gray-900/50 rounded-lg gap-2 sm:gap-3">
                <div class="flex-1 min-w-0">
                  <span class="text-white font-semibold text-sm sm:text-base block break-words">{item.products?.name || `Producto #${item.product_id}`}</span>
                  <span class="text-white/70 text-xs sm:text-sm block mt-1">Cantidad: {item.quantity}</span>
                </div>
                <span class="font-bold text-primary text-base sm:text-lg flex-shrink-0 self-start sm:self-center">${Number(item.total_price).toLocaleString('es-CL')}</span>
              </div>
            ))}
          </div>
        )}
      </div>
    ) : (
      <div class="bg-gray-900/50 border-2 border-red-500 rounded-xl p-4 sm:p-6 md:p-8 shadow-lg backdrop-blur-sm">
        <div class="text-center">
          <h2 class="text-xl sm:text-2xl font-bold text-white mb-2">Pedido no encontrado</h2>
          <p class="text-white/90 mb-3 sm:mb-4 text-sm sm:text-base">
            {orderError ? `Error: ${orderError.message}` : 'No se encontró un pedido con ese ID.'}
          </p>
          <a href="/" class="inline-block px-4 sm:px-6 py-2 sm:py-3 bg-primary text-white rounded-lg hover:bg-primary-600 transition-colors text-sm sm:text-base">
            Volver al inicio
          </a>
        </div>
      </div>
    )}
  </section>
</BaseLayout>
